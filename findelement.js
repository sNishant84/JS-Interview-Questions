// find an element in sorted array . so what we can do is find the min index then we can apply binary search on 
// both section one start from o to index-1
// second from index to N -1 . one will return the answer