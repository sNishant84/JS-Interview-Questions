// in this we have given strings we will take two string basically same same if we find lcs we have all but we have to remove that
//is only once basically on the same index for that we can put condition i==j so not include so in lcs
// if(a[i-1]==b[j-1] && i!=j) that is the answer